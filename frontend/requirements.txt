# ================================================================
# MockPilot AI — Requirements (Streamlit Cloud)
# ================================================================
# ⚠️  Streamlit Cloud runs the FRONTEND only.
#     Deploy the FastAPI backend separately (e.g. Render / Railway)
#     and set BACKEND_URL in your Streamlit Secrets accordingly.
# ================================================================


# ── Streamlit Frontend ────────────────────────────────────────────
streamlit==1.35.0


# ── AI / LLM (Groq Cloud API) ────────────────────────────────────
# Used directly by backend service modules imported in the frontend
groq>=0.13.0


# ── Resume Parsing ────────────────────────────────────────────────
# frontend/views/resume_analyzer.py imports these directly from
# backend.services.resume_service (no API hop — runs client-side)
pdfplumber==0.11.0
PyMuPDF==1.24.4
python-docx==1.1.2


# ── HTTP Client ───────────────────────────────────────────────────
httpx>=0.28.0,<1.0
requests==2.32.3


# ── Analytics & Charts ───────────────────────────────────────────
plotly==5.22.0
pandas==2.1.4


# ── Validation & Settings ────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.1
email-validator==2.1.1


# ── Utilities ────────────────────────────────────────────────────
python-dotenv==1.0.1
python-dateutil==2.9.0
