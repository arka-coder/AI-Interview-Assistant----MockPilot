# ================================================================
# MockPilot AI — Frontend Requirements (Streamlit Cloud)
# ================================================================
# ⚠️  Streamlit Cloud runs the FRONTEND only.
#     The FastAPI backend is deployed separately on Render.
#     Set BACKEND_URL in Streamlit Cloud → App Settings → Secrets:
#
#       BACKEND_URL = "https://your-app.onrender.com"
#
# ⚠️  Backend-only packages (fastapi, uvicorn, sqlalchemy, PyMuPDF,
#     pdfplumber, etc.) are NOT needed here — the frontend calls the
#     backend API for all heavy processing.
# ================================================================


# ── Streamlit Frontend ────────────────────────────────────────────
streamlit==1.35.0


# ── AI / LLM (Groq Cloud API) ────────────────────────────────────
groq>=0.13.0


# ── HTTP Client ───────────────────────────────────────────────────
httpx>=0.28.0,<1.0
requests>=2.32.0


# ── Analytics & Charts ───────────────────────────────────────────
plotly>=5.22.0
pandas>=2.1.0


# ── Validation & Settings ────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0


# ── Utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0
python-dateutil>=2.9.0
