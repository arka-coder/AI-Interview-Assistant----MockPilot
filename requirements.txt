# ================================================================
# MockPilot AI — Frontend Requirements (Streamlit Cloud)
# ================================================================
# ⚠️  Streamlit Cloud runs the FRONTEND only.
#     The FastAPI backend is deployed separately on Render.
#     Set BACKEND_URL in Streamlit Cloud → App Settings → Secrets:
#
#       BACKEND_URL = "https://your-app.onrender.com"
#
# ⚠️  Backend-only packages (fastapi, uvicorn, sqlalchemy, etc.)
#     are NOT listed here — see requirements-backend.txt for Render.
# ================================================================


# ── Streamlit Frontend ────────────────────────────────────────────
streamlit==1.35.0


# ── AI / LLM (Groq Cloud API) ────────────────────────────────────
groq>=0.13.0


# ── Resume Parsing ────────────────────────────────────────────────
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
python-docx>=1.1.0


# ── HTTP Client ───────────────────────────────────────────────────
httpx>=0.28.0,<1.0
requests==2.32.3


# ── Analytics & Charts ───────────────────────────────────────────
plotly==5.22.0
pandas>=2.1.0


# ── Validation & Settings ────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator==2.1.1


# ── Utilities ────────────────────────────────────────────────────
python-dotenv==1.0.1
python-dateutil==2.9.0
